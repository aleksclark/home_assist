esphome:
  name: ble-scanner-bedroom
  friendly_name: BLE Scanner Bedroom

esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: esp-idf

logger:
  level: INFO

api:
  encryption:
    key: !secret api_key

ota:
  platform: esphome
  password: !secret ota_password

wifi:
  ssid: "ClarkUltra"
  password: "deadbeef00"
  fast_connect: true

  ap:
    ssid: "BLE-Bedroom-Fallback"
    password: "fallback123"

captive_portal:

esp32_ble_tracker:
  scan_parameters:
    interval: 1100ms
    window: 1100ms
    active: true

bluetooth_proxy:
  active: true
