esphome:
  name: ble-scanner-carport
  friendly_name: BLE Scanner Carport

esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: esp-idf

logger:
  level: INFO

api:
  encryption:
    key: !secret api_key

ota:
  platform: esphome
  password: !secret ota_password

wifi:
  ssid: "ClarkUltra"
  password: "deadbeef00"
  fast_connect: true

  ap:
    ssid: "BLE-Carport-Fallback"
    password: "fallback123"

captive_portal:

esp32_ble_tracker:
  scan_parameters:
    interval: 1100ms
    window: 1100ms
    active: true

bluetooth_proxy:
  active: true
